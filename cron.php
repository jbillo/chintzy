<?php
/*
 * Here are all the tasks that need to be run periodically:
 * :XXX: This is all to be done
 * :FIXME: Yes, make sure to fix this file
 *
 * Rotate log entries for sitelogs table and /log/ directory.
 * Remove expired cookies from the user_cookies table.
 * Remove expired account reset tokens from the user_recovery table. (once per day)
 * Reset all account reset attempt counts from the user_recovery table. (once per day)
 * module: store_registry: Check registry_address_map table for registry_id values that no longer exist to see if any are being orphaned.
 */
 
 include_once './includes/init.php';
 
 
