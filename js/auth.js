$(document).ready(function() {
    // $('form[method=post]').append('<input type="hidden" name="token" value="' + TOKEN + '" />');
    // $('form[class!=inline]').append('<div class="no-csrf"><img src="' + ROOT + 'images/script-code.png" /> This form uses a <a href="' + ROOT + 'admin/help#csrf_token">validation token</a> to protect your data.</div>');
});